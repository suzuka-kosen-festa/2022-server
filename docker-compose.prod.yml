web:
  build: 
    context: .
    dockerfile: ./Dockerfiles/prod.Dockerfile
  volumes:
    - ./app

db:
  volumes:
    - postgres_volume:/var/lib/postgresql/data

volumes:
    postgres_volume: